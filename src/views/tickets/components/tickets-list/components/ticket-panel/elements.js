import styled from 'styled-components';

const Actions = styled.div`
  display: flex;
  padding-top: 10px;
  border-top: 1px solid #ecedef;

  button {
    margin-right: 5px;
  }
`;

export { Actions };
