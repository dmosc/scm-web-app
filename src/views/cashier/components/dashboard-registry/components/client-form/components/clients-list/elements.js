import styled from 'styled-components';

const TitleContainer = styled.div`
  display: flex;
  justify-content: space-between;
  width: 100%;
`;

const TitleList = styled.h1`
  text-transform: uppercase;
  font-weight: bold;
`;

export { TitleContainer, TitleList };
