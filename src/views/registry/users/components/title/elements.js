import styled from 'styled-components';

const TitleContainer = styled.div`
  display: flex;
  align-items: center;
  padding: 10px;

  > * {
    margin-right: 5px;
  }
`;

export default TitleContainer;
